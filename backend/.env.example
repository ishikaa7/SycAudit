# Copy this file to .env and fill in real values. Never commit .env itself.

ENVIRONMENT=development

# Postgres — used by the app (async driver)
DATABASE_URL=postgresql+asyncpg://sycaudit:changeme@localhost:5432/sycaudit
# Same DB, sync driver — used only by Alembic migrations
DATABASE_URL_SYNC=postgresql+psycopg://sycaudit:changeme@localhost:5432/sycaudit

# LLM provider keys
GROQ_API_KEY=
GEMINI_API_KEY=
HUGGINGFACE_API_KEY=

JWT_SECRET=change-this-to-a-real-random-secret
